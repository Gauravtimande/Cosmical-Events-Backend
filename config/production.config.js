export const config = {
	host: "https://cep.arcassetz.in",
	port: 19088,
	baseUrl: "",
	db: {
		host: "localhost",
		port: 3306,
		username: "Gaurav",
		database: "Cosmical-Events",
		password: "Gaurav@123"
	  }
  };
  config.baseUrl = `${config.host}:${config.port}/`;
  